# Production Environment Configuration
# Copy this file to .env in your production environment
# DO NOT COMMIT ACTUAL SECRETS TO GIT

# --- General ---
NODE_ENV=production
# Frontend URL (Users access this)
FRONTEND_URL=https://your-domain.com
# Backend Public URL (If different, usually same as FRONTEND_URL/api if proxied, or explicit API domain)
BACKEND_BASE_URL=https://your-domain.com/api

# --- Database (PostgreSQL) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public
# In Docker Compose, HOST is usually the service name (e.g., 'postgres')
DATABASE_URL=postgresql://postgres:secure_password_here@postgres:5432/bazi_master?schema=public
# Allow switching Prisma provider to PostgreSQL at runtime if needed (though we patched Dockerfile)
# PRISMA_PROVIDER=postgresql 

# --- Cache & Session (Redis) ---
# Format: redis://HOST:PORT
REDIS_URL=redis://redis:6379

# --- Security ---
# Comma-separated list of admin emails
ADMIN_EMAILS=admin@your-domain.com,operator@your-domain.com
# Session idle timeout in ms (default 24h)
# SESSION_IDLE_MS=86400000

# --- AI Providers (Optional) ---
# One or more of: openai, anthropic
AVAILABLE_PROVIDERS=openai,anthropic
# API Keys (Leave empty if not using)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-...

# --- OAuth (Optional) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://your-domain.com/api/auth/google/callback

WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_REDIRECT_URI=https://your-domain.com/api/auth/wechat/callback

# --- Rate Limiting ---
FORCE_RATE_LIMIT=true
